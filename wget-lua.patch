diff -rupN ./src/luahooks.c ../wget-1.14.lua/src/luahooks.c
--- src/luahooks.c	2013-05-02 13:30:23.000000000 -0700
+++ src/luahooks.c	2014-02-10 19:37:18.810476693 -0800
@@ -144,7 +144,8 @@ luahooks_init ()
   if (opt.lua_filename == NULL)
     return;
 
-  lua = lua_open ();
+  /* Fix for lua >=5.2 */
+  lua = luaL_newstate ();
   luaL_openlibs (lua);
 
   /* Initialize constants. */
